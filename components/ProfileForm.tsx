import React from 'react';

// Placeholder component - not currently used in the app flow
export const ProfileForm: React.FC = () => {
  return (
    <div className="flex items-center justify-center min-h-screen bg-gray-50 dark:bg-gray-900">
      <div className="text-center p-8">
        <h2 className="text-2xl font-bold text-gray-800 dark:text-gray-200">Profile Form</h2>
        <p className="mt-4 text-gray-600 dark:text-gray-400">This component is not currently used.</p>
      </div>
    </div>
  );
};
